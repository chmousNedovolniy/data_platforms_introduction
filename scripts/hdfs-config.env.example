# Hosts (FQDN или IP)
NAMENODE_HOST=nn1.example.com
SECONDARY_NN_HOST=sn1.example.com
DATANODE_HOSTS=(dn1.example.com dn2.example.com dn3.example.com)

# SSH доступ
SSH_USER=ubuntu
SSH_KEY=~/.ssh/id_rsa
SSH_OPTS="-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null"

# Hadoop
HADOOP_VERSION=3.3.6
HADOOP_TGZ_URL=https://dlcdn.apache.org/hadoop/common/hadoop-3.3.6/hadoop-3.3.6.tar.gz
# Опционально можно указать sha256 и раскомментировать проверку в скрипте:
# HADOOP_TGZ_SHA256=<sha256>
HADOOP_PREFIX=/opt/hadoop
HADOOP_USER=hadoop

# Директории данных и логов (на каждой ноде)
HDFS_NAME_DIR=/hadoop/name
HDFS_SECONDARY_DIR=/hadoop/secondary
HDFS_DATA_DIR=/hadoop/data
HADOOP_LOG_DIR=/var/log/hadoop

# Java
JAVA_HOME=/usr/lib/jvm/java-11-openjdk-amd64

# Порты
NAMENODE_RPC_PORT=8020
NAMENODE_HTTP_PORT=9870
SECONDARY_HTTP_PORT=9868
DATANODE_HTTP_PORT=9864
DATANODE_TRANSFER_PORT=9866
DATANODE_IPC_PORT=9867

# Репликация
DFS_REPLICATION=3
